#[derive(Debug, Clone, Default)]
struct Eval {
 signals: Vec<char>,
 numbers: Vec<i8>,
 result: i8,
}


fn eval_formula(formula: &str) -> bool{
    let mut eval = Eval{
        signals: Vec::new(),
        numbers: Vec::new(),
        result: 0i8,
    };
    
    while (formuala)
    
    if eval.result == 1{
        true
    }else{
        false
    }
}

fn main() {
    println!("Hello, world!");
}
